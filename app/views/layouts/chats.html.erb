<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Los Gehts" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="container-fluid d-flex vh-100">
      <div class="row align-self-strech w-100">
        <div class="col-md-2 mx-auto bg-dark text-white">
          <div class="w-100 text-align-center d-flex justify-content-center mt-3">
            <%= form_with(model: Chat.new) do |form| %>
              <%= form.hidden_field :history, value: "" %>
              <%= form.submit("+ New chat", class:"mt-3 mt-3-dark border-light") if current_user %>
            <% end %>
          </div>
          <div class="mt-3">
            <h4>Today:</h4>
            <turbo-frame id="user-chats">
              <%= render partial: "chats/chat_list", collection: current_user.chats, as: :chat %>
            </turbo-frame>
          </div>
        </div>
        <div class="col-10">  
          <%= yield %>
        </div>
      </div>
    </div>
  </body>
</html>
